# ドローン運用管理ツール 仕様書

版数：第1版（ドラフト）　作成日：2026年6月13日

---

## 1. 概要

### 1.1 目的
本ツールは、ドローン（無人航空機）の運用に関わる以下の業務をブラウザ上で一元管理するための静的Webアプリケーション（PWA）である。

- 機体・操縦者情報の管理
- 飛行計画書の作成・印刷（PDF出力）
- 飛行前点検（チェックリスト）の実施・記録
- 飛行日誌（様式１）の記録・印刷
- 飛行記録データの検索・集計・バックアップ
- 飛行計算（飛行時間・GSD等）の参考算出

### 1.2 対象ユーザー
ドローンを業務・個人で運用する操縦者・運航管理者。航空法に基づく飛行計画・記録の作成を簡便化したいユーザー。

### 1.3 想定環境
- 公開先：GitHub Pages（静的サイト、サーバーサイド処理なし）
- 対応ブラウザ：モダンブラウザ（Chrome / Edge / Safari 等）。PWAとしてホーム画面に追加可能。
- データ保存：すべてブラウザの `localStorage`（一部 `sessionStorage`）に保存。外部サーバーへの送信は行わない。

### 1.4 技術構成
- HTML / CSS / JavaScript（フレームワーク不使用、Vanilla JS）
- 共通スタイルシート：`theme.css`
- 共通計算ロジック：`flight-calc.js`
- PWA関連：`manifest.json`、`sw.js`（Service Worker、オフライン対応）

---

## 2. システム構成

### 2.1 ページ一覧

| ファイル名 | 画面名 | 概要 |
|---|---|---|
| `index.html` | ホーム | 各ツールへのナビゲーション、全体ワークフローの説明 |
| `aircraft.html` | 機体管理 | 機体の登録・編集・削除、総飛行時間の表示 |
| `flight-plan.html` | 飛行計画書 | 飛行計画の作成・印刷（PDF）、チェックリストへの引き継ぎ |
| `checklist.html` | 飛行前チェックリスト | 飛行前確認6項目＋日常点検9項目の実施・印刷（実施年月日＋登録記号ごとに複数登録） |
| `flight-log.html` | 飛行日誌 | 飛行記録（様式１）の入力・1日分の印刷／JSON出力 |
| `data.html` | データ管理 | 計画書／チェックリスト／日誌の3タブ管理・検索・集計・CSV/JSON出力・全データ一括入出力・全データ消去 |
| `calculator.html` | 飛行計算（参考機能） | 飛行時間・VLOS距離・GSDの参考算出、法令簡易チェック |

### 2.2 画面遷移・ワークフロー

本ツールの中核となる業務フローは以下の通り、3画面が連携している。

```
飛行計画書（flight-plan.html）
   │  「この計画で飛行前チェックリストへ進む」
   ▼
飛行前チェックリスト（checklist.html）
   │  （チェック完了後、飛行日誌へ手動遷移）
   ▼
飛行日誌（flight-log.html）
```

計画書で入力した内容（案件名・飛行日・場所・操縦者・機体等）は `drone-active-session`（localStorage）を介して、チェックリストおよび日誌に自動的に引き継がれる。

機体管理（aircraft.html）で登録した機体・操縦者情報は、飛行計画書・飛行日誌の入力候補（ドロップダウン）として参照される。

データ管理（data.html）と飛行計算（calculator.html）は、上記フローを補助する独立機能として位置づけられる。

---

## 3. 画面仕様

### 3.1 ホーム（index.html）

各ツールへのリンクカードを表示する。カードには各ツールの名称・概要・アイコンを表示し、以下6項目への遷移を提供する。

- 機体管理：機体・操縦者情報の登録・編集
- 飛行計画書：飛行内容を入力してPDFの飛行計画書を作成
- 飛行前チェックリスト：飛行前点検（自動保存）
- 飛行日誌：飛行記録の累積管理
- データ管理：記録の検索・CSV/JSON出力
- 飛行計算（参考機能）：飛行時間・GSD等の参考算出

ページ下部に、計画書→チェックリスト→日誌の一連の流れと、計画書の内容が自動的に引き継がれることを説明する案内文を表示する。

### 3.2 機体管理（aircraft.html）

#### 機能概要
機体（無人航空機）の登録・編集・削除を行い、登録済み機体ごとの総飛行時間を表示する。

#### 入力フォーム項目

| 項目 | 必須 | 内容 |
|---|---|---|
| 機体名 | ○ | 例：DJI Mavic 3 Enterprise（重複登録不可） |
| 機体番号（JU番号） | － | 登録記号 |
| 担当操縦者 | － | 主な操縦者氏名 |
| 総飛行時間の繰越値（分） | － | 本ツール導入前までの飛行時間（分単位、0以上の整数） |

#### 一覧表示・機能
- 登録済み機体を一覧表示。各機体について「機体名」「JU番号（未登録の場合は『JU番号未登録』表示）」「担当操縦者」「総飛行時間」を表示。
- **総飛行時間 ＝ 繰越値 ＋ 飛行日誌に記録された当該機体の飛行時間の合計**。一覧には繰越分と日誌集計分の内訳を併記する。
- 各機体に「編集」「削除」ボタンを設置。編集時はフォームに値を読み込み、見出しを「機体の編集：〇〇」に変更。削除時は確認ダイアログを表示（日誌の過去記録は削除されない旨を明示）。
- 機体名の重複チェックを行い、登録済み名称との重複時はアラート表示。
- 飛行日誌の記録が追加・削除されると、本画面の総飛行時間表示も自動的に再計算される（機体名の文字列一致により集計）。

#### JSON入出力（機体管理データ）
- **JSON出力**：登録済み機体の一覧を JSON ファイルとして書き出す。出力形式は `{ app: 'drone-aircraft', exportedAt: <ISO日時>, aircraft: [...] }`。ファイル名は `機体管理_YYYY-MM-DD.json`。
- **JSON読込**：JSON ファイルを読み込み、機体管理データをインポートする。配列形式（`aircraft` 配列、またはトップレベル配列）に対応。**重複は後勝ち**（機体名が一致する場合は後から読み込んだデータで上書き、一致しない場合は新規追加）。読み込み後に新規／上書き件数をアラート表示する。

---

### 3.3 飛行計画書（flight-plan.html）

#### 機能概要
飛行計画の内容を入力し、A4縦のフォーマットでプレビュー表示・印刷（PDF出力）する。入力内容はドラフトとして自動保存され、チェックリストへ引き継ぐことができる。

#### 画面構成
左側：入力フォーム　／　右側：入力内容を反映したリアルタイムプレビュー（印刷時のレイアウトと同一）

#### 入力項目

| 項目 | 内容 |
|---|---|
| 案件名 | 飛行計画のタイトル |
| 飛行日 | 飛行実施日 |
| 飛行開始時刻／終了時刻 | 予定時刻 |
| 飛行場所（住所） | 飛行を実施する場所 |
| 操縦者氏名 | 「選択または自由入力」の単一プルダウン（飛行日誌の[機体]欄と同仕様）。機体管理の担当操縦者から選択、または「＋手入力…」で直接入力 |
| 機体（最大5機） | 各行が「選択または自由入力」のプルダウン（機体名）＋JU番号欄。機体管理の登録機体から選択（選択時はJU番号を自動補完）、または「＋手入力…」で直接入力。最大5行まで追加可能 |
| 飛行目的 | 飛行の目的（点検・撮影・測量等） |
| 備考 | 自由記述欄。特定飛行に関するクイック挿入ボタン（＋夜間／＋目視外／＋25kg以上／＋物件投下）、および「📋 飛行計算の結果を備考に挿入」ボタン（calculator.htmlで算出した内容を反映）を提供 |

#### 機能
- 入力内容は `drone-flight-plan-draft`（localStorage）に自動保存され、再訪時に復元される。
- 「保存」「読み込み」によりJSONファイル（app識別子：`drone-flight-plan`）として計画を保存・復元可能。
- 「印刷／PDF出力」ボタンにより、A4縦のフォーマットで印刷ダイアログを起動（ブラウザのPDF保存機能でPDF化）。
- 「この計画で飛行前チェックリストへ進む」ボタンにより、現在の入力内容を `drone-active-session`（`step: 'checklist'`、計画内容を含む）として保存し、checklist.htmlへ遷移する。

---

### 3.4 飛行前チェックリスト（checklist.html）

#### 機能概要
飛行前確認事項6項目と、国土交通省様式２（日常点検記録）に基づく9項目（計15項目）を記録する。**[実施年月日]＋[無人航空機の登録記号]の組み合わせを1件のキーとして複数登録に対応**する。データは `drone-checklist-records`（localStorage・配列）に保存し、チェックリストタブでは[実施年月日]の1日分のみを絞り込んで表示・編集する。一覧・削除・出力はデータ管理タブで行う。進捗は「◯/15」形式で表示される。

#### 機体概要グループ（複数登録キー・他タブから初期値転記）
項目順序：①実施年月日 ②実施場所 ③無人航空機の登録記号 ④実施者。以下を初期値として自動転記し、手動上書きも可能。

| 項目 | 転記元 |
|---|---|
| 実施年月日 | 計画書タブの[飛行日]（なければ当日） |
| 実施場所 | 計画書タブの[飛行場所] |
| 無人航空機の登録記号 | 機体管理タブの[機体番号]（計画書1機目に一致する登録機体、なければ先頭） |
| 実施者 | 機体管理タブの[担当操縦者] |

#### 飛行前確認事項6項目（独自追加。外観点検・バッテリー残量・昼間飛行・目視内飛行は様式２に存在しないため削除）

| キー名 | 項目 |
|---|---|
| `pilot_condition` | 操縦者の体調確認（飲酒・薬物なし） |
| `weather_check` | 気象状況の確認（風速・天候） |
| `airspace_check` | 飛行空域の確認（DID・150m制限・空港周辺など） |
| `surroundings_check` | 周囲の安全確認（第三者・障害物） |
| `special_application` | 特例申請の確認（包括／個別／承認の区分ガイダンス表を表示） |
| `police_contact` | 地元警察への事前連絡（注記：平日午後4時締切） |

#### 日常点検9項目（様式２準拠）

**[結果]プルダウンは廃止し、チェックボックスのオン＝正常／オフ＝異常で扱う。** 備考欄はチェックがオフ（異常）の場合のみ表示する（既定は正常＝オン）。

| キー名 | 点検項目（日本語／英語） |
|---|---|
| `uas_general` | 機体全般（UAS GENERAL） |
| `propeller` | プロペラ（PROPELLER(S)） |
| `frame` | フレーム（FRAME） |
| `communication_system` | 通信系統（COMMUNICATION SYSTEM） |
| `propulsion_system` | 推進系統（PROPULSION SYSTEM） |
| `power_system` | 電源系統（POWER SYSTEM） |
| `automatic_control_system` | 自動制御系統（AUTOMATIC CONTROL SYSTEM） |
| `flight_control_system` | 操縦装置（FLIGHT CONTROL SYSTEM） |
| `battery_fuel` | バッテリー・燃料（BATTERY, FUEL） |

#### データ構造（`drone-checklist-records` 配列の各要素）
`{ id, date, registration, place, inspector, notes, checks: { <上記キー>: boolean }, remarks: { <日常点検キー>: '異常内容' } }`。JSON出力時のチェック項目キー名は上表の英語短縮名を用いる（旧 `chk-1`…の連番形式は廃止）。

#### 機能
- 入力は (実施年月日＋登録記号) ごとに自動保存（upsert）される。日付・登録記号を変えると、その組み合わせの保存内容を呼び出す（なければチェックを初期化した新規点検）。
- 飛行計画書から遷移した場合、`drone-active-session` の内容を画面上部に表示し、当日の新規点検を開始する。
- データ管理タブからの編集要求（`drone-checklist-edit-request`・sessionStorage）で特定の点検を開く。
- 「印刷」は選択中の[実施年月日]の全件を、[登録記号]ごとにページを分けて出力する。各ページに様式２（日常点検記録）に加え、飛行前確認事項・特記事項・実施場所/年月日/実施者をリスト形式で印刷する。
- 旧キー `drone-checklist-state`（単一状態）は廃止。互換のための自動移行は行わない（過去の点検チェック内容は引き継がれない。実施場所等の様式２記録は新規入力となる）。

---

### 3.5 飛行日誌（flight-log.html）

#### 機能概要
国土交通省様式１（飛行記録、Journey Log of UAS）に基づく飛行記録の入力・蓄積・印刷・データ出力を行う。**記録一覧は[飛行年月日]の1日分のみを絞り込んで表示する。**

#### 入力グループと項目（1フライトごと・記録に保存）
入力フォームは「環境概要」「機体概要」「記録の入力」の3グループで構成される。

**環境概要グループ**（項目順）
| 項目 | 内容 |
|---|---|
| 飛行概要（総括） | 計画書の[案件名]を初期表示（新規項目） |
| 飛行年月日 | 日付。1日分の絞り込みキー |
| 飛行場所 | 計画書の[飛行場所]を初期表示（新規項目） |
| 天候／風速 | 気象情報 |

**機体概要グループ**（項目順：機体／登録記号／繰越総飛行時間／操縦者名）
機体は「選択または自由入力」プルダウン。機体選択時に機体管理タブのデータを自動上書き入力する。
| 自動入力先 | 取得元（機体管理タブ） |
|---|---|
| 無人航空機の登録記号 | 機体番号 |
| 繰越総飛行時間 | 繰越総飛行時間 |
| 飛行させた者の氏名 | 担当操縦者 |

**記録の入力グループ**
| 項目 | 内容 |
|---|---|
| 離陸時刻／着陸時刻 | 「記録」ボタンで現在時刻を自動入力。両者から飛行時間（分）を自動算出 |
| 離陸場所／着陸場所 | 「飛行場所をコピー」（環境概要の[飛行場所]を転記）「GPS座標を取得」ボタンあり |
| 飛行概要（NATURE OF FLIGHT） | 飛行内容の記述。「飛行概要をコピー」ボタンで[飛行概要（総括）]の値を転記 |
| 異常事項（MATTERS AFFECTED FLIGHT SAFETY） | 安全に影響した事項 |
| 確認者（CONFIRMER） | 確認者氏名 |
| メモ | 自由記述 |

- **[バッテリー番号]欄は削除**（メモ欄との重複のため）。
- **[記事（不具合記録）SQUAWK]は[記録を追加]ボタンの下へ移動し、コメントアウトで非表示**（当面「不具合なし」扱い。将来有効化前提でコードは残置）。
- 旧ヘッダー（`drone-flight-log-header`：登録記号・繰越総飛行時間）は廃止し、機体概要グループの記録項目（registration / carryover）として各レコードに保存する。

#### データ管理機能
- **JSON出力（この1日分）**：選択中の[飛行年月日]の計画書・チェックリスト・日誌記録をまとめて1ファイルに出力（app識別子：`drone-daily-record`、version 2。checklist は当日分の `drone-checklist-records` を内包）。
- **印刷**：選択中の[飛行年月日]の日誌を[機体番号（登録記号）]単位でページを分けてリスト印刷（様式１・A4横）。
- **入力欄リセット（データクリア）**：このタブの入力欄のみをリセットする（保存済み記録は削除しない）。
- 以下は廃止し、データ管理タブへ移管：CSV書出／JSON読込／データ管理検索リンク／全記録バックアップ／バックアップ通知（meta）／全データ削除。

#### 連携機能
- 飛行計画書からの引き継ぎ：`drone-active-session` の内容を新規記録の初期値として反映。
- データ管理画面からの編集要求：`drone-edit-request`（sessionStorage）を介して特定記録の編集画面を開く。

---

### 3.6 データ管理（data.html）

#### 機能概要
3つのタブ（**データ管理（計画書）／データ管理（チェックリスト）／データ管理（日誌）**）でデータを管理し、加えて全データの一括バックアップ・全データ消去を提供する。

#### データ管理（計画書）タブ
- `drone-flight-plan-draft`（計画書の下書き1件）の内容をプレビュー表示。
- **JSON出力（計画書）**：`{ app:'drone-flight-plan', plan }` を出力。
- **JSON読込（計画書）**：計画書JSONを読み込み、下書きを置き換える。
- ※ 計画書の複数管理は将来対応予定（データ構造・キー設計を拡張しやすい形で維持）。

#### データ管理（チェックリスト）タブ
- `drone-checklist-records`（実施年月日＋登録記号ごと）の一覧表示（実施年月日／登録記号／実施場所／実施者／異常件数）。
- 各件の「編集」（`drone-checklist-edit-request` を介してチェックリストタブで開く）「削除」。
- **JSON出力（チェックリスト）**：`{ app:'drone-checklist', checklist }` を出力。
- **JSON読込（チェックリスト）**：IDによる重複判定でマージ（後勝ち）。

#### データ管理（日誌）タブ
- **検索・絞り込み**および**総飛行時間の集計**は初期状態では折りたたみ（非表示）。利用者が任意に展開できる。
- 検索項目：期間（開始日～終了日）／飛行場所／機体／キーワード（飛行概要・メモ・異常記録）。
- 集計：機体ごとの総飛行時間（繰越＋日誌集計）。DIPS申請時の参考情報。
- **CSV出力**：BOM付きUTF-8。出力列＝飛行年月日／操縦者／離陸時刻／着陸時刻／飛行時間／離陸場所／着陸場所／機体／天候／風速／飛行概要／異常記録／メモ（バッテリー列は廃止）。
- **JSON出力（日誌）**：表示中（絞り込み結果）をJSON出力。
- **JSON読込**：複数ファイル対応。`recordKey()`（IDまたは複合キー）による重複判定で後勝ちマージ。離陸日時→操縦者の順でソート。
- **印刷**：飛行日誌形式（様式１相当）をA4横で、機体ごとにページを分けて出力。各行に累積総飛行時間を表示。

#### 全データ（一括）
- **全データJSON出力**：計画書・チェックリスト・日誌・機体管理を1ファイルにまとめて出力（app識別子：`drone-all-data`、version 1。構造 `{ data: { aircraft, plan, checklist, records } }`）。
- **全データJSON読込**：確認ダイアログののち、各データセットを読み込んだ内容で置き換える。
- **全データ消去**：計画書・チェックリスト・日誌・機体管理の全データ（および旧キー・セッション情報）を削除。誤操作防止のため確認ダイアログを表示（飛行日誌タブから移管）。

---

### 3.7 飛行計算（参考機能）（calculator.html）

#### 機能概要
飛行計画書・飛行日誌の作成に必須ではない、参考算出のためのツール。3つのセクションから構成される。

#### ①飛行時間・ホバリング計算
機体の質量・搭載物重量・ローター数・プロペラ直径（インチ→m変換）・バッテリー容量／電圧・FoM（推力効率）・駆動効率・バッテリー健全度・予備バッテリー比率・巡航係数等から、推定飛行時間を算出（`flight-calc.js` の `calcFlightTime`）。

- 機体プリセット：入力値のドラフトを `drone-calc-draft` に保存。ユーザー独自プリセットの保存・読込（`saveUserPresets` / `loadUserPresets`）。
- 飛行日誌の実績データ（`flightLogStats()`）と算出値を比較表示。

#### ②VLOS（目視外飛行）距離算出・航空法簡易チェック
- `visualLineOfSightM` により目視外飛行の参考距離を算出。
- `updateCompliance()` により、以下の簡易判定を表示。
  - 機体重量25,000g以上：機体認証が必要
  - 機体重量100g以上：登録・リモートID（Remote ID）が必要
  - 飛行高度150mを超える：個別許可が必要
  - 飛行高度130mを超える：警告表示
  - VLOS距離に関する警告表示

#### ③GSD（地上画素解像度）・写真測量計算
- センサープリセット（`SENSOR_PRESETS`）、`calcGsd`、`altitudeForGsdM` によりGSDと飛行高度の関係を算出。
- 前後・左右オーバーラップ率を入力し、動的SVGによる可視化（`updateViz`）を表示。
- 「計画をコピー」ボタンで算出結果をクリップボードにコピー。
- 「飛行計画に適用」ボタンで算出結果を `drone-calc-summary` に保存し、flight-plan.htmlへ遷移（備考欄に挿入可能）。

---

## 4. データ仕様（localStorageキー一覧）

| キー名 | 保存場所 | 内容 | 主な利用画面 |
|---|---|---|---|
| `drone-aircraft-list` | localStorage | 機体・操縦者の登録情報 | aircraft.html（読み書き）／flight-plan.html・flight-log.html（読み取り） |
| `drone-flight-log-records` | localStorage | 飛行記録（様式１相当）の全件。各レコードに登録記号・繰越総飛行時間を含む | flight-log.html（読み書き）／aircraft.html・data.html（読み取り） |
| `drone-flight-plan-draft` | localStorage | 飛行計画書の入力ドラフト | flight-plan.html（読み書き）／data.html（読み書き） |
| `drone-checklist-records` | localStorage | チェックリスト（実施年月日＋登録記号ごと）の全件配列 | checklist.html（読み書き）／data.html（読み書き）／flight-log.html（日次JSON出力で読み取り） |
| `drone-active-session` | localStorage | 計画書→チェックリスト→日誌の引き継ぎ情報（手順・計画内容） | flight-plan.html → checklist.html → flight-log.html |
| `drone-calc-draft` | localStorage | 飛行計算の入力ドラフト | calculator.html |
| `drone-calc-summary` | localStorage | 飛行計算結果（飛行計画への適用用） | calculator.html → flight-plan.html |
| `drone-edit-request` | sessionStorage | データ管理から日誌記録への編集要求 | data.html → flight-log.html |
| `drone-checklist-edit-request` | sessionStorage | データ管理からチェックリストへの編集要求 | data.html → checklist.html |

※ 廃止したキー：`drone-flight-log-header`（登録記号・繰越総飛行時間はレコードへ統合）／`drone-flight-log-meta`（バックアップ通知機能の廃止）／`drone-checklist-state`（複数登録化により `drone-checklist-records` へ移行）。
※ 一括バックアップのファイル形式 `drone-all-data`（`{ data: { aircraft, plan, checklist, records } }`）は出力／読込専用で、localStorageキーとしては保存しない。

---

## 5. 印刷／PDF出力仕様

| 画面 | 出力内容 | 用紙設定 |
|---|---|---|
| flight-plan.html | ドローン飛行計画書 | A4縦 |
| checklist.html | 様式２（日常点検記録）＋飛行前確認事項＋特記事項。実施年月日の全件を登録記号ごとにページ分割 | A4縦 |
| flight-log.html | 様式１（飛行記録）。選択中の飛行年月日を機体番号ごとにページ分割 | A4横 |
| data.html | 飛行日誌一覧（機体ごとにページ分割、累積飛行時間付き） | A4横 |

いずれもブラウザの印刷機能を利用し、「PDFに保存」を選択することでPDFファイルとして出力できる。

---

## 6. 制約事項・前提条件

- すべてのデータはブラウザのlocalStorage/sessionStorageに保存されるため、**ブラウザのキャッシュ削除・別端末／別ブラウザの利用時にはデータが引き継がれない**。定期的なJSON/CSVエクスポート（特にデータ管理タブの「全データJSON出力」）によるバックアップが必須。
- **localStorageに関する警告について**：通常利用ではブラウザ警告は発生しない。報告される警告は、プライベート／シークレットモード、サイトデータ・Cookieのブロック設定、広告ブロック等の拡張機能、ストレージ容量上限など、ブラウザの設定・拡張・モードに起因する個別事象と考えられる。localStorageを使用しないとブラウザを閉じた時点でデータが消失するため代替手段はとらない。利用者には「通常モードで開き、サイトデータ保存を許可する／こまめに全データJSON出力でバックアップする」よう案内する（userguide 8.1参照）。
- サーバーサイドでのデータ保存・共有機能は提供しない。複数人での記録共有はJSONファイルの受け渡し・取込により行う。
- 機体の紐付けは機体名の文字列一致により行われるため、機体名の変更・重複には注意が必要。
- 本ツールの算出値（飛行時間・GSD・VLOS等）はあくまで参考値であり、実際の運用判断・法令適合性の最終確認は操縦者・運航管理者の責任で行うこと。
- 航空法・DIPS等の規定は変更される可能性があるため、最新の法令・MLIT発表内容を別途確認すること。

---

## 7. 今後の拡張に向けた留意点

- 本書は土台（ドラフト）であり、実装の詳細変更（フィールド追加・画面追加等）に応じて随時更新が必要。
- 将来的にクラウド同期・複数アカウント対応等を検討する場合は、データ構造（localStorageキー）の互換性維持またはマイグレーション方針を別途検討すること。
