# ドローン運用管理ツール ユーザー・マニュアル

版数：第1版（ドラフト）　作成日：2026年6月13日

---

## 1. はじめに

本ツールは、ドローンの運用に関わる「機体管理」「飛行計画書作成」「飛行前点検」「飛行日誌記録」「データ管理」「飛行計算」をブラウザ上で行うためのWebアプリケーションです。インターネット環境がなくても、一度開いたページはオフラインでも利用できます（PWA対応）。

データはすべてお使いの端末・ブラウザ内（localStorage）に保存されます。サーバーには送信されません。そのため、**こまめなバックアップ（JSON/CSV出力）を強く推奨します。**

### おすすめの利用フロー

```
① 機体管理で機体・操縦者を登録
② 飛行計画書を作成して印刷（PDF化）
③「チェックリストへ進む」ボタンで飛行前点検を実施
④ 飛行日誌に記録を入力・保存
⑤ 必要に応じてデータ管理で検索・集計・バックアップ
```

---

## 2. 機体管理（aircraft.html）の使い方

### 2.1 機体を新規登録する
1. ホーム画面から「機体管理」を開きます。
2. 「機体の登録」フォームに以下を入力します。
   - **機体名**（必須・例：DJI Mavic 3 Enterprise）
   - 機体番号（JU番号）
   - 担当操縦者
   - 総飛行時間の繰越値（分）※本ツールを使い始める前までの飛行時間を分単位で入力（任意）
3. 「この内容で登録する」ボタンを押します。

> 同じ機体名は重複して登録できません。

### 2.2 機体情報を編集する
1. 一覧から対象機体の「編集」ボタンを押します。
2. フォームに既存の内容が読み込まれるので、修正して「この内容で更新する」を押します。
3. 編集を中止する場合は「編集をやめる」を押します。

### 2.3 機体を削除する
1. 一覧から対象機体の「削除」ボタンを押し、確認ダイアログで「OK」を選択します。
2. ※ 削除しても、飛行日誌に記録済みの過去データは削除されません。

### 2.4 総飛行時間の確認
各機体カードには「総飛行時間（繰越 ＋ 日誌の累計）」が自動表示されます。飛行日誌に記録を追加・削除すると、この表示も自動的に更新されます。

### 2.5 機体データのバックアップ・取り込み（JSON）
- **JSON出力**：「登録済みの機体」見出し横の「JSON出力」ボタンを押すと、登録した機体一覧が JSON ファイル（`機体管理_日付.json`）として保存されます。別端末への移行やバックアップに利用します。
- **JSON読込**：「JSON読込」ボタンを押し、出力した JSON ファイルを選択すると、機体データを取り込めます。**同じ機体名がすでにある場合は、読み込んだ内容で上書き**されます（後勝ち）。読み込み後、新規・上書きの件数が表示されます。

---

## 3. 飛行計画書（flight-plan.html）の使い方

### 3.1 計画を作成する
1. ホーム画面から「飛行計画書」を開きます。
2. 左側のフォームに以下を入力します。
   - 案件名、飛行日、飛行開始・終了時刻
   - 飛行場所（住所）
   - 操縦者氏名（1行のプルダウン。機体管理の担当操縦者から選択、または「＋手入力…」で直接入力）
   - 機体（最大5機まで追加可。各行のプルダウンで登録機体から選択するとJU番号も自動入力。「＋手入力…」で直接入力も可）
   - 飛行目的
   - 備考（特定飛行の種類を選ぶと自動で文言が挿入されるボタンあり：＋夜間／＋目視外／＋25kg以上／＋物件投下）
3. 右側にリアルタイムで印刷時のレイアウトが表示されます。

入力内容は自動的に保存されるため、ページを閉じても次回開いたときに復元されます。

### 3.2 飛行計算の結果を計画書に反映する
飛行計算（calculator.html）で算出した内容がある場合、「📋 飛行計算の結果を備考に挿入」ボタンを押すと、算出結果が備考欄に自動で挿入されます。

### 3.3 計画書を印刷・PDF化する
1. 「印刷／PDF出力」ボタンを押します。
2. 印刷ダイアログが開くので、出力先を「PDFに保存」に変更して保存します（A4縦）。

### 3.4 計画をファイルに保存・読み込む
- 「JSON保存」ボタンでJSONファイルとして計画内容を保存できます。
- 「JSON読込」ボタンで、保存したJSONファイルから計画を復元できます。
- 計画書データはデータ管理タブ（計画書）からも出力・読み込みできます。

### 3.5 飛行前チェックリストへ進む
入力が完了したら「この計画で飛行前チェックリストへ進む」ボタンを押します。計画内容が自動的にチェックリスト画面へ引き継がれます。

---

## 4. 飛行前チェックリスト（checklist.html）の使い方

### 4.1 画面の構成・複数登録のしくみ
飛行計画書から進んだ場合、画面上部に計画の概要が表示されます。チェック項目は全15項目で、上部に「◯/15」の進捗が表示されます。

このチェックリストは、**[実施年月日]と[無人航空機の登録記号]の組み合わせを1件**として複数登録できます。日付・登録記号を変えると、その組み合わせの保存内容が呼び出されます（保存がなければ新しい点検として開始）。同じ日に複数の機体を点検する場合は、登録記号を切り替えてそれぞれ記録します。すべての点検の一覧・削除はデータ管理タブ（チェックリスト）で行います。

### 4.2 機体概要を入力する
「機体概要」グループに、以下の順で入力します。計画書・機体管理の値が初期表示され、手動で上書きできます。

1. 実施年月日（計画書の飛行日／なければ当日）
2. 実施場所（計画書の飛行場所）
3. 無人航空機の登録記号（機体管理の機体番号）
4. 実施者（機体管理の担当操縦者）

### 4.3 飛行前確認事項6項目をチェックする
以下の6項目を確認し、チェックを入れます。

1. 操縦者の体調確認（飲酒・薬物なし）
2. 気象状況の確認（風速・天候）
3. 飛行空域の確認（DID・150m制限・空港周辺など）
4. 周囲の安全確認（第三者・障害物）
5. 特例申請の確認（包括／個別／承認のどれに該当するか、案内表を参照）
6. 地元警察への事前連絡（注：平日午後4時までの連絡が必要です）

### 4.4 日常点検9項目（様式２）を記録する
以下の9項目を点検します。**チェックを入れた状態＝正常**、**チェックを外した状態＝異常**として扱います（初期状態は全項目「正常」）。異常（チェックを外した）項目のみ、備考欄が表示されるので内容を記入してください。

1. 機体全般 / 2. プロペラ / 3. フレーム / 4. 通信系統 / 5. 推進系統 / 6. 電源系統 / 7. 自動制御系統 / 8. 操縦装置 / 9. バッテリー・燃料

### 4.5 自動保存・リセット
入力はその都度、現在の（実施年月日＋登録記号）の記録として自動保存されます。「この点検をリセット」ボタンで、表示中の点検のチェックと特記事項を初期化できます。

### 4.6 印刷する
「印刷」ボタンを押すと、選択中の[実施年月日]の全件が、[登録記号]ごとにページを分けてA4縦で出力されます。各ページには様式２（日常点検記録）に加え、飛行前確認事項・特記事項・実施場所/年月日/実施者が印刷されます。

---

## 5. 飛行日誌（flight-log.html）の使い方

### 5.1 入力フォームの構成
フォームは「環境概要」「機体概要」「記録の入力」の3グループに分かれています。記録一覧は、入力中の「飛行年月日」1日分のみが表示されます。

### 5.2 環境概要を入力する
- 飛行概要（総括）：計画書の案件名が初期表示されます
- 飛行年月日（この日付の記録が一覧に表示されます）
- 飛行場所：計画書の飛行場所が初期表示されます
- 天候・風速

### 5.3 機体概要を入力する
「機体」を選ぶと、機体管理タブのデータから**登録記号・繰越総飛行時間・操縦者名が自動入力**されます（手修正可）。機体は一覧から選択するか「＋手入力…」で直接入力できます。

### 5.4 飛行記録を入力する
1. 離陸・着陸時刻は、その場でボタンを押すと現在時刻が自動入力されます。両方入力すると飛行時間が自動計算されます。
2. 離陸場所・着陸場所は、「飛行場所をコピー」ボタンで環境概要の[飛行場所]を呼び出せます。「GPS座標を取得」で現在地も取得できます。
3. 飛行概要（NATURE OF FLIGHT）は「飛行概要をコピー」ボタンで環境概要の[飛行概要（総括）]を転記できます。
4. 異常事項・確認者・メモを入力し、「記録を追加」で確定します。

> ※ バッテリー番号欄は廃止しました（必要な場合はメモ欄をご利用ください）。記事（不具合記録・SQUAWK）は当面「不具合なし」扱いとして非表示にしています。

### 5.5 この1日分を出力・印刷する
- **JSON出力**：選択中の飛行年月日1日分の計画書・チェックリスト・日誌をまとめて1ファイルで保存します。
- **印刷**：選択中の1日分を、機体番号（登録記号）ごとにページを分けて様式１（飛行記録）でA4横印刷します。

### 5.6 入力欄をリセットする
「入力欄をリセット」ボタンは、このタブの入力欄だけを空にします（**保存済みの記録は消えません**）。記録の検索・バックアップ・全データ消去はデータ管理タブで行います。

---

## 6. データ管理（data.html）の使い方

データ管理は「計画書」「チェックリスト」「日誌」の3つのタブに分かれています。

### 6.1 データ管理（計画書）タブ
- 現在の計画書（下書き）の内容を確認できます。
- 「JSON出力（計画書）」で保存、「JSON読込（計画書）」で復元できます。
- ※ 計画書の複数管理は将来対応予定です。

### 6.2 データ管理（チェックリスト）タブ
- 実施年月日＋登録記号ごとのチェックリスト一覧を表示します。
- 各件の「編集」でチェックリストタブが開き、「削除」で個別に削除できます。
- 「JSON出力／読込（チェックリスト）」でバックアップ・復元できます。

### 6.3 データ管理（日誌）タブ
- **「検索・絞り込み」と「総飛行時間の集計」は初期状態では閉じています。** 見出しを押すと開きます。
- 検索：期間・飛行場所・機体・キーワードで絞り込めます。
- 集計：機体ごとの総飛行時間（繰越＋日誌の累計）を表示します（DIPS申請の参考に）。
- 「CSV書き出し」「JSON出力（日誌）」で表示中の結果を出力できます。
- 「JSON読込」で複数ファイルをまとめて取り込めます（同じ記録は後勝ちで上書き）。
- 「印刷」で様式１相当の飛行日誌をA4横・機体ごとにページを分けて出力します。

### 6.4 全データの一括バックアップ・消去
- **全データJSON出力**：計画書・チェックリスト・日誌・機体管理の全データを1ファイルにまとめて保存します。端末の引っ越しや丸ごとバックアップに使えます。
- **全データJSON読込**：確認のうえ、全データを読み込んだ内容で置き換えます。
- **全データ消去**：全データを削除します（確認ダイアログあり）。**取り消せません。必ず先にバックアップしてください。**

---

## 7. 飛行計算（参考機能）（calculator.html）の使い方

> ※本機能は参考値の算出のみを目的としており、飛行計画書・飛行日誌の作成には必須ではありません。

### 7.1 飛行時間・ホバリングの計算
1. 機体の質量、搭載物重量、ローター数、プロペラ直径、バッテリー容量・電圧などを入力します。
2. 自動的に推定飛行時間が算出されます。
3. よく使う機体の設定は「プリセット」として保存・呼び出しができます。
4. 飛行日誌の実績データと比較表示されます。

### 7.2 VLOS距離・航空法簡易チェック
1. 機体重量・飛行高度・飛行距離などを入力します。
2. 目視外飛行の参考距離（VLOS距離）が算出されます。
3. 以下のような簡易判定が表示されます。
   - 機体重量25,000g以上 → 機体認証が必要
   - 機体重量100g以上 → 登録・リモートID登載が必要
   - 飛行高度150m超 → 個別の許可申請が必要
   - 飛行高度130m超 → 警告表示

> ※この判定は簡易的な参考情報です。実際の申請要否は必ず最新の航空法・DIPSの内容で確認してください。

### 7.3 GSD（地上画素解像度）・写真測量計算
1. カメラのセンサーサイズ（プリセットから選択可）、焦点距離、画素数などを入力します。
2. 目標とするGSDから必要な飛行高度を算出、または飛行高度からGSDを算出できます。
3. 前後・左右のオーバーラップ率を入力すると、撮影範囲のイメージ図（SVG）が表示されます。
4. 「計画をコピー」ボタンで算出結果をコピーできます。
5. 「飛行計画に適用」ボタンを押すと、算出結果が飛行計画書の備考欄に反映されます（飛行計画書画面へ移動します）。

---

## 8. データのバックアップと復元（全体まとめ）

本ツールのデータはブラウザ内にのみ保存されるため、以下の点に注意してください。

- **ブラウザのキャッシュ・履歴を削除すると、データが消える可能性があります。**
- 別の端末・別のブラウザでは、データは共有されません。
- 機種変更やブラウザの再インストール前には、必ずデータ管理画面の**「全データJSON出力」**で全データ（計画書・チェックリスト・日誌・機体管理）を1ファイルに保存してください。
- 個別に保存したい場合は、各タブのJSON出力・CSV書き出しも利用できます。

保存したJSONファイルは、データ管理画面の各タブの「JSON読込」または「全データJSON読込」から読み込むことで復元できます。

### 8.1 「localStorageに関する警告」が出る場合について
本ツールはデータ保存にブラウザ標準の `localStorage` を使用しています。通常の利用でブラウザの警告は発生しませんが、ごく一部の環境で警告が表示されるという報告があります。想定される原因は次のとおりです。

- **プライベート／シークレットモード**で開いている（保存が制限される／閉じると消える）
- ブラウザのプライバシー設定で**サイトデータ・Cookieをブロック**している
- **広告ブロック等の拡張機能**がストレージ利用を警告・制限している
- ストレージ容量の上限に達している

**ご案内（推奨対応）：** 本ツールは通常モードで開き、サイトデータの保存を許可してご利用ください。`localStorage` を使わないとブラウザを閉じた時点で入力内容がすべて消えてしまうため、本ツールでは代替しません。大切な記録は、こまめに「全データJSON出力」でファイルとして保存してください。

---

## 9. よくある質問（FAQ）

**Q. 入力したデータが消えてしまいました。**
A. ブラウザのキャッシュ削除や別ブラウザでのアクセス、プライベートモードでの利用が原因の可能性があります。事前にエクスポートしたJSONファイルがあれば、データ管理画面の「全データJSON読込」または各タブの「JSON読込」から復元してください。バックアップがない場合、データの復旧はできません（8.1も参照）。

**Q. 機体管理に登録した機体が、飛行計画書のドロップダウンに表示されません。**
A. 機体管理画面で機体名が正しく保存されているか確認してください。ページの再読み込みも試してください。

**Q. 飛行計算の結果を計画書に反映したい。**
A. 飛行計算画面で算出後、「飛行計画に適用」または「計画をコピー」ボタンを使用し、飛行計画書画面の「📋 飛行計算の結果を備考に挿入」ボタンで反映できます。

**Q. オフラインでも使えますか。**
A. 一度オンラインで開いたページは、Service Workerによりオフラインでも利用可能です。ただし、初回アクセスはオンライン環境が必要です。

---

## 10. 注意事項

- 本ツールで算出・表示される数値（飛行時間・GSD・VLOS距離・法令適合の簡易判定等）はあくまで参考情報です。実際の運用・申請にあたっては、必ず最新の航空法・MLIT・DIPSの内容をご自身で確認してください。
- 本マニュアルは初期版（ドラフト）です。ツールの機能変更に応じて、随時更新してください。
